[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "shvix"
version = "0.1.0"
description = "Local MLX recovery daemon for shmerminal/openclaw"
requires-python = ">=3.10"
dependencies = [
    "mlx-lm>=0.20",
]
