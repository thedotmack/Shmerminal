{
  "name": "shvix",
  "version": "0.1.0",
  "description": "Local recovery agent for shmerminal/openclaw — offline, cross-platform via Ollama",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "shvix": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.6.0"
  }
}
